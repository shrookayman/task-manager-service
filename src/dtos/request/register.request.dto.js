// import { IsEmail, IsNotEmpty, MinLength, MaxLength } from 'class-validator';
// import {PASSWORD_REGX} from './../../utils/validator'

// export class RegisterDto { 
//   @IsNotEmpty()
//   @MinLength(2, 'Name Must be at least 2 characters')
//   @MaxLength(2, 'Name can not exceed 50 characters')
//   name;

//   @IsEmail()
//   email;

//   @MinLength(6)
//   @Matches( PASSWORD_REGX,{message:'Password must contain uppercase, lowercase, number, and special character'})
//   password;
// }
